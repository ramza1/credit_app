<div class="item clearfix">
  <div class="info" style="padding: 0;">
    <a href="#" class="name">You have loaded your <%= order.item_type.downcase %></a>
    <p>Amount:<b><%= order.amount %></b></p>
    <span><%= order.created_at.day %> <%= MONTH[order.created_at.month-1]%>  <%=order.created_at.strftime("%Y") %> <%=order.created_at.strftime("%I:%M%p") %></span>
  </div>
</div>