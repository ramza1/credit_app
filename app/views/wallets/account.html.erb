<% content_for :navigation do %>
    <%= currently_at 'My Wallet' %>
    </div>
<% end %>
<div class="page-header">
  <h1>My Wallet </h1>
</div>

<div class="row-fluid">
  <div class="tab-host clearfix">
    <ul class="nav nav-tabs">
      <li class="active" >
        <a href="<%= url_for account_user_wallet_url(current_user) %>">
          Account Balance
          <div class="tape"></div>
        </a>
      </li>
      <li >
        <a href="<%= url_for load_money_user_wallet_url(current_user) %>">
          Load Money
          <div class="tape"></div>
        </a>
      </li>
    </ul>
  </div>
  <div class="block messages ui-helper-clearfix">
       <h3><%= number_to_currency current_user.wallet.account_balance, unit: "₦" , precision: 0%>
         <a class="btn btn-primary btn-large pull-right" href="<%= url_for load_money_user_wallet_url(current_user) %>">
           <i class="icon-money"></i> Load Money
         </a></h3>
       <h4></h4>
  </div>
</div>


