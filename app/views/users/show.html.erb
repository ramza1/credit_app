<% content_for :navigation do %>
    <%= currently_at 'My Profile' %>
    </div>
<% end %>
<div class="page-header">
  <h1>My Profile<%= link_to "Edit Profile", edit_user_path(@user), class: "pull-right btn " %></span> </h1>
        </div>

        <div class="row-fluid">
          <div class="head clearfix">
            <h1><i class="icon-user"></i> Profile Info</h1>
          </div>

          <div class="block-fluid ucard">

            <div class="info">
              <ul class="rows rounded_bottom letter_pressed">
                <li>
                  <div class="title">Phone Number:</div>
                  <div class="text"><%= @user.phone_number %></div>
                </li>
                <li>
                  <div class="title">Email:</div>
                  <div class="text"><%= @user.email %></div>
                </li>
                <li>
                  <div class="title">Sms Notification</div>
                  <div class="text"><%= @user.sms_notification? ? "Yes" : "No" %></div>
                </li>
              </ul>
            </div>
          </div>
        </div>